
(let [x 4]
  x)

#_(
  x = 4
  x)


(let [x 4
      x 5
      n []]
  (+ (.length n) x))

x = 4
x = 5
n = []

n.length.send

; ISeq#eval continues to eval in current scope even when moved elsewhere.
;
;
; scrap ^

(let [x 4]
  x)

resolve let to rouge.builtins/let
which is a builtin

so this form can be compiled to a lambda

; vim: set ft=clojure:
